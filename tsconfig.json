{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/*": ["./*"], // Root-level imports (components/, hooks/, etc)
      "@/src/*": ["src/*"] // src/ imports
    }
  },
  "include": ["**/*.ts", "**/*.tsx", ".expo/babel.config.ts"]
}
